This folder holds static assets for the app (images, fonts, icons).

Place your icon at `assets/icon.png` and any images under `assets/images/`.

Example:
- assets/icon.png
- assets/images/logo.png

Note: this README is a placeholder — add actual image files as needed.
